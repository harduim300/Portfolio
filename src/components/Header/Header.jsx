import style from "./Header.module.scss"

const Header = () => {
    return (
        <header>
            <div className={style.headerContainer}>
                <h1>Em breve Header !</h1>
            </div>
        </header>
    );
}

export default Header;